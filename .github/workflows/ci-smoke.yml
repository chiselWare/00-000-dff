name: CI Smoke (calls org reusable workflow)

on:
  pull_request:
  workflow_dispatch:

jobs:
  call-smoke:
    uses: chiselWare/ci-workflows/.github/workflows/smoke-test.yml@v1
    with:
      message: "Hello from 00-000-dff calling chiselWare/ci-workflows"
    secrets: inherit
